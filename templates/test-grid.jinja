<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>title</title>
    <style>
        :root {
        --num-cols: 4;
        --gap: 2%; /* 2% de largeur du conteneur */
        }

        .grid-container {
        display: grid;
        grid-template-columns: repeat(var(--num-cols), calc((100% - var(--gap) * (var(--num-cols) - 1)) / var(--num-cols)));
        gap: var(--gap);
        width: 100%;
        border: 2px solid black;
        }

        .grid-item {
        background: lightblue;
        padding: 20px;
        text-align: center;
        border: 1px solid navy;
        }
    </style>
</head>
<body>
    <div class="grid-container">
        <div class="grid-item">1</div>
        <div class="grid-item">2</div>
        <div class="grid-item">3</div>
        <div class="grid-item">4</div>
      </div>
</body>
</html>